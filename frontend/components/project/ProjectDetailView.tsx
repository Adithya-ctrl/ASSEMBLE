"use client";

import { Check, CheckCircle, Code, WarningCircle, XCircle } from "@phosphor-icons/react";
import { Button } from "@radix-ui/themes";
import Link from "next/link";

import type { CreateProjectResponse } from "../../lib/types";
import { humanize } from "../../lib/ui";

export default function ProjectDetailView({ response, onOpenInspector }: { response: CreateProjectResponse; onOpenInspector: () => void }) {
  const project = response.project;
  const ready = project.status === "READY";
  return <article className={`project-detail project-detail-route ${ready ? "project-detail-ready" : "project-detail-not-ready"}`} aria-labelledby="project-detail-title"><div className="project-detail-hero"><div><p className="section-kicker">{ready ? "Ready for delivery" : "Readiness gaps remain"}</p><h2 id="project-detail-title">{project.title}</h2><p>{project.short_description}</p></div><span className={`project-ready-badge ${ready ? "" : "project-not-ready-badge"}`}>{ready ? <CheckCircle aria-hidden="true" size={18} weight="fill" /> : <WarningCircle aria-hidden="true" size={18} weight="fill" />}{project.status}</span></div><div className="project-objective"><strong>Objective</strong><p>{project.objective}</p></div><dl className="project-facts"><div><dt>When</dt><dd>{humanize(project.schedule.start_slot)} to {humanize(project.schedule.end_slot)} / {project.schedule.duration_slots} slots</dd></div><div><dt>Where</dt><dd>{project.venue.venue_name} / capacity {project.participant_capacity}</dd></div><div><dt>Host organisation</dt><dd>{project.host_organisation_name}</dd></div><div><dt>Proof state</dt><dd>{project.base_state_id === project.verified_state_id ? "Verified baseline" : "Verified updated state"}</dd></div></dl><div className="project-detail-grid"><section aria-labelledby="operational-team-title"><h3 id="operational-team-title">Operational team</h3><ul className="assignment-list">{project.operational_assignments.map((assignment) => <li key={assignment.role_id}><span className="assignment-status"><Check aria-hidden="true" size={15} weight="bold" /></span><div><strong>{assignment.role_label}</strong><span>{assignment.person_name} / {assignment.organisation_name}</span><small>{[...assignment.person_capabilities, ...assignment.person_languages].map(humanize).join(" / ") || "Availability matched"}</small></div></li>)}</ul></section><section aria-labelledby="readiness-title"><h3 id="readiness-title">Readiness checks</h3><ul className="readiness-list">{project.readiness.checks.map((check) => <li className={check.ready ? "readiness-ready" : "readiness-missing"} key={check.check_id}>{check.ready ? <CheckCircle aria-hidden="true" size={17} weight="fill" /> : <XCircle aria-hidden="true" size={17} weight="fill" />}<div><strong>{check.ready ? "Ready: " : "Missing: "}{check.label}</strong><span>{check.evidence.map(humanize).join(" / ") || "No supporting evidence returned"}</span></div></li>)}</ul></section></div><div className="project-detail-grid project-detail-grid-compact"><section><h3>Resources and access</h3>{project.resources.map((resource) => <p key={resource.resource_id}><strong>{resource.resource_name}</strong><br />{resource.quantity_required} allocated / {resource.quantity_available} available</p>)}<p><strong>Accessibility</strong><br />{project.accessibility_requirements.map(humanize).join(", ") || "No additional requirements declared"}</p></section><section><h3>Capabilities and languages</h3><p><strong>Modules</strong><br />{project.capability_modules.map(humanize).join(", ")}</p><p><strong>Operational languages</strong><br />{project.supported_languages.map(humanize).join(", ")}</p></section></div><footer className="project-proof-footer"><span>{ready ? <CheckCircle aria-hidden="true" size={16} weight="fill" /> : <WarningCircle aria-hidden="true" size={16} weight="fill" />}Fresh server verification: {response.verification.status} / Project {project.status}</span><span className="source-plan-control"><Button onClick={onOpenInspector} size="2" type="button" variant="outline"><Code aria-hidden="true" size={16} />Open Technical Inspector</Button><Link className="secondary-link" href="/projects/proof">View Project proof</Link></span></footer></article>;
}
